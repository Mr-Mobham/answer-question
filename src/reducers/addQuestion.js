const addQuestion = (state = {},action) => {

    switch (action.type) {
           case 'ADD-QUESTION':
          
               return {
               };

        

        default : 
        return state;
     
    }
}


export default addQuestion;